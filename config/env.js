const ip = '127.0.0.1'
const username = 'admin'
const password = 'admin'

module.exports = {
  // databases: `mongodb://${username}:${password}@${ip}:27017/goods`,
  databases: `mongodb://${ip}:27017/goods`,
}
